
<%= form_with model: @team do |form| %>

	<p>
		<%= form.label :Name %>
		<%= form.select :sponsor_id, options_for_select(Sponsor.all.map{|b| [b.name, b.id]}) %>
	</p>
	<p><%= form.submit %></p>
<%end%>

<p><%= link_to 'Back', team_path %></p>